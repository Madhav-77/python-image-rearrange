{
    "version": 2,
    "builds": [{
        "src": "api/main.py",
        "use": "@vercel/python"
    }],
    "routes": [{
        "src": "/api/(.*)",
        "dest": "/api/main.py"
    }],
    "env": {
        "DATABASE_URL": "postgres://user:password@hostname:port/database_name"
    }
}